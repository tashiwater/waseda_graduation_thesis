#!/bin/sh
# model select
CURRENT_DIR=$(cd $(dirname $0); pwd)
SRC_DIR="${CURRENT_DIR}/../src/"
model_dir="${SRC_DIR}/model/"
# cd "${SRC_DIR}/model/"
cp  
# SCRIPT_DIR=$(cd $(dirname $0); pwd)

# echo $SCRIPT_DIR
ls
#cp "${model_dir}/" "${model_dir}"